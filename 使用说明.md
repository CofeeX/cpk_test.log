# CPK-PCBA 测试分析系统 · 使用说明

CPK-PCBA 是一个基于 Flask 的测试数据分析系统：上传 Excel 测试记录后，自动计算平均值、标准差、CP、CPK、合格率等统计指标，并绘制趋势图与分布图。

- 运行方式：`python app.py`，然后浏览器访问 `http://127.0.0.1:5000`
- 依赖安装：`pip install -r requirements.txt`
- 远程仓库：`https://github.com/redbiger/cpk_test.log`

---

## 局域网服务器部署

工程可部署到局域网内一台服务器电脑，让多台电脑通过浏览器直接访问使用。

### 部署准备（在服务器电脑上执行一次）
1. 安装 Python 3.13 并加入系统 PATH（https://www.python.org/downloads/）
2. 拷贝或克隆工程代码到服务器（如 D:\CPK-PCBA）：
   ```bash
   git clone https://github.com/redbiger/cpk_test.log.git CPK-PCBA
   cd CPK-PCBA
   ```
3. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```

### 启动服务
双击 `start.bat`，或在命令行执行：
```bash
python app.py --serve
```
生产模式使用 waitress 服务器（线程数 8），监听 0.0.0.0:5000。关闭启动窗口即停止服务。

### 防火墙放行（一次性，需管理员权限）
```powershell
netsh advfirewall firewall add rule name="CPK-PCBA" dir=in action=allow protocol=TCP localport=5000
```

### 客户端访问
局域网内其他电脑浏览器打开：`http://服务器IP:5000`
（服务器上执行 `ipconfig` 查看 IP）

### 数据目录
- 数据库：`data\test_analysis.db`（上传历史记录）
- 上传文件与临时数据：`data\uploads\`
- 产品配置：`configs\`（需保持可写，"保存产品配置"功能需要）
- 建议定期备份整个 `data\` 目录

---

## 一、三端协作模式

工程在本机、GitHub 仓库、外地电脑三处维护，**GitHub 是中央仓库**，两端电脑不直接交换文件，一切改动通过 `pull` / `push` 中转：

```
本机  ──push──▶  GitHub 仓库  ◀──push──  外地电脑
本机  ◀──pull──  GitHub 仓库  ──pull──▶  外地电脑
```

铁律：**编辑前先 `git pull`，改完立即 `git push`**，让 GitHub 始终保持最新，避免两端代码分叉。

## 二、外地电脑首次设置

1. 安装 Git：https://git-scm.com/download/win
2. 安装 Python 3.13：https://www.python.org/downloads/
3. 克隆工程（仓库是公开的，克隆不需要账号）：

```bash
cd D:\Code
git clone https://github.com/redbiger/cpk_test.log.git CPK-PCBA
cd CPK-PCBA
```

4. 配置身份（不配置无法提交）：

```bash
git config user.name "redbiger"
git config user.email "317815094+redbiger@users.noreply.github.com"
```

5. 安装依赖并启动：

```bash
pip install -r requirements.txt
python app.py
```

## 三、日常同步循环（两台电脑相同）

```bash
git pull origin main      # ① 先拉取他人改动，避免冲突
# ... 编辑代码 ...
git add .                 # ② 暂存改动
git commit -m "改了什么"   # ③ 本地提交（描述要写清楚）
git push origin main      # ④ 推送到 GitHub
```

## 四、推送认证（Personal Access Token）

仓库是公开的，克隆无需认证；但**推送（push）必须登录**，推荐使用 Token：

1. GitHub 网页 → 右上角头像 → **Settings** → 左侧 **Developer settings** → **Personal access tokens** → **Tokens (classic)** → **Generate new token**
2. 勾选 `repo` 权限，有效期建议 90 天，生成后**立即复制保存**（只显示一次）
3. push 时提示输入：
   - 用户名：`redbiger`
   - 密码：粘贴上面生成的 Token

## 五、注意事项

- **网络超时**：访问 github.com 可能很慢。若 push/pull 超时失败，执行一次：

  ```bash
  git config --global http.connectTimeout 120
  git config --global http.lowSpeedLimit 1
  git config --global http.lowSpeedTime 60
  ```

- **避免同改一个文件**：两台电脑同时修改同一文件并先后 push，后 push 的一方会冲突。务必遵循"先 pull 后 push"。
- **运行数据不上传**：`.gitignore` 已排除 `data/`（Excel 与数据库）、`dist/`、`build/`、`*.exe`、`__pycache__/` 等。两端只同步代码，测试数据各自独立，互不干扰。
- **配置跟代码走**：`configs/` 目录（如 SC-POWER板测试记录.json）随代码同步，两端保持一致，请勿在单机随意改动。
- **EXE 打包产物不入库**：发布用的 EXE 由 `build_exe.spec` 在本地打包生成，不推送到仓库。

## 六、常用命令速查

| 操作 | 命令 |
|---|---|
| 查看改动 | `git status` |
| 查看修改内容 | `git diff` |
| 查看提交历史 | `git log --oneline` |
| 放弃未提交改动 | `git checkout -- <文件名>` |
| 拉取远端更新 | `git pull origin main` |
| 推送本地提交 | `git push origin main` |
