# 应用名称
产品功能测试分析
## 需求描述
倒入Excel格式的测试记录，按测试项目生成曲线并统计分析这些性能。

## 功能列表
- 功能1：网页版应用，以后可部署到服务器端
- 功能2：输入为excel的文件记录
- 功能3：根据不同产品类别需要不同的解析入口
- 功能4：能显示每个项目的曲线图和统计分析
- 功能5：能选择不同测试项目，筛选出目标项目
- 功能6：能按产品类别保存配置，便于后续同款产品直接调用配置

## 页面说明
- 首页：显示文件选择、分析按钮、测试项目的曲线图
- 详情页：显示本文件中包含的产品数量、测试项目数量、测试项目的曲线图

## 技术要求
- 语言：Python / HTML+JS
- 数据来源：Excel文件
- 前端：Bootstrap
- 后端：Flask
- 数据库：MySQL
- 图表库：ECharts
- 统计分析：Pandas
- 配置管理：JSON

## excel数据表格式说明
- 单个数据表为一种产品的测试记录
- 每个数据表包含多个产品的记录
- 每个产品为一行记录，每个产品有多项测试项目，每个产品的记录从“序列号”开始
- 测试项目按列区分，其中包含：项目名称/项目备注/参数变量/规格上限/规格下限等等
- 测试项目的测试值内容如果是文本型则不需要解析，如果是数字型则需要解析显示

## 数据提取规则
应用导入 Excel 测试记录文件后，按以下 4 条规则提取数据并生成图表：

1. **读取文件**
   - 读取 Excel 工作表 (默认第 1 个 sheet)
   - 若用户已选择产品配置 (`产品类别`)，按配置解析；否则调用 `ExcelParser.auto_detect_config` 自动检测配置 (扫描 `Program Name` 标题行、规格上下限标签、固定列名)
   - 输出长格式 DataFrame：每行一个 (产品, 测试项目) 数值记录

2. **提取公共信息 + 产品数量**
   - **公共信息**：
     - 从顶部跳过行 (`skip_top_rows`) 中提取 `Program Name` 等标题字段 (支持 `Key: Value` 同单元格 与 `Key:` 单元格 + 右侧值 两种写法)
     - 从首行产品记录中提取固定列值：`工单号` / `测试员` / `工装编号` / `结论` / `时间`
   - **产品数量**：按 `序列号` (`sequence` 列) 去重计数

3. **有上下限的项目列为分析数据 + 统计项目数量**
   - 候选范围：解析出的所有数值型测试项目
   - **筛选规则**：仅保留同时满足以下条件的测试项目
     - 至少有规格上限或规格下限其中之一 (`spec_lower` 或 `spec_upper` 不为 `None`)
     - 对应数据为数值型 (非文本/非空)
   - 无规格限的项目不参与图表展示
   - **分析项目数量** = 筛选后剩余的测试项目数

4. **合并上下限一致的项目为一组 + 统计图表数量**
   - 按 `(spec_lower, spec_upper)` 元组对分析项目分组
   - 元组相同的项目合并为同一组 (规格下限或上限为 `None` 时也参与分组，`None` 与 `None` 视为相同)
   - 每组生成 1 张图表，图表内包含 N 条曲线 (N = 该组内项目数)
   - **图表数量** = 分组后的组数
   - **曲线总数** = 分析项目数量 (与规则3 输出一致)

### 规则执行示例 (SC板测试记录01.xlsx)
| 规则 | 输出 |
|------|------|
| 1 读取文件 | 106,380 行 × 13 列 (长格式) |
| 2 公共信息 | Program Name=2001-01015-00-EU SC-POWER板-260304, 工单号=W11-26010037, 产品数量=647 |
| 3 分析项目数量 | 53 项 (有上下限 + 数值列; 120 项无规格限被排除) |
| 4 图表数量 | 32 张 (9 张多曲线合并图, 23 张单曲线图; 曲线总数=53) |

### 真实 Excel 结构 (参考: 测试数据/SC板测试记录01.xlsx)
- 第 1 行: 程序标题行 (如 `Program Name: 2001-01015-00-EU SC-POWER板-260304`)，需跳过
- 第 2-6 行: 测试项目元数据表头 (共 5 行)
  - 第 2 行: 项目名称 (同一项目可能占多列, 如 `静态负载测试_16` 占 4 列)
  - 第 3 行: 项目备注
  - 第 4 行: 参数变量 (如 `CH1电压(V)` / `CH1电流(V)` / `量测值(V)`，作为子项区分多列同名项目)
  - 第 5 行: 规格上限 (`*` 或 `-` 表示无限)
  - 第 6 行: 规格下限 (`*` 或 `-` 表示无限)
- 第 7 行: 固定列名行 (序列号/工单号/名称/测试员/工装编号/结论/时间)
- 第 8 行起: 产品测试记录 (每行一个产品，从序列号开始)
- 单元格内容可能带单引号前缀 (如 `'单通道电源设定_1'`)，解析时需去除

### 配置字段说明 (configs/*.json)
- `skip_top_rows`: 起始跳过的标题行数 (SC 板 = 1)
- `header_rows`: 测试项目元数据占用行数 (默认 5)
- `meta_row_mapping`: 元数据行索引映射 (item_name/item_note/param_variable/spec_upper/spec_lower)
- `fixed_columns_header_row`: 固定列名行号 (相对跳过行后, SC 板 = 5; -1 表示不用)
- `fixed_columns`: 固定列字段映射 (sequence/product_model/work_order/tester/fixture/result/time)
- `test_items`: 关注测试项目白名单 (空数组 = 解析全部; 非空 = 仅解析指定项目, 用于调试或聚焦分析)

## 图表显示规范
### 曲线图元素
- 主曲线: 测试记录数据点连线 (带数据缩放、保存图片、区域填充)
- 规格上限参考线: 绿色实线 `#198754`，标注 `上限 {数值}`
- 规格下限参考线: 绿色实线 `#198754`，标注 `下限 {数值}`
- 均值参考线: 粉红色虚线 `#e91e63`，标注 `均值 {数值}`

### Y 轴坐标范围
- 最小值 = `min(规格下限, 记录值最小值)`，再下浮 8% 边距
- 最大值 = `max(规格上限, 记录值最大值)`，再上浮 8% 边距
- 确保所有参考线及其标签完整显示，不被裁剪

## 工程目录结构
```
traecode-测试分析001/
├── app.py                          # Flask 主应用（路由+API）
├── config.py                       # 应用配置（数据目录、上传目录、密钥）
├── models.py                       # 数据库模型（UploadRecord）
├── start.bat                       # Windows 启动脚本
├── requirements.txt                # Python 依赖清单
├── 测试分析001.md                   # 本说明文档
├── 测试数据/
│   └── SC板测试记录01.xlsx          # 示例测试数据
├── configs/
│   └── SC-POWER板测试记录.json     # 产品配置（可选，留空则自动提取）
├── services/
│   ├── excel_parser.py             # Excel 解析（含 auto_detect_config）
│   ├── analyzer.py                 # 图表数据生成（按规格自动分组）
│   └── config_manager.py            # 配置管理
├── templates/
│   ├── base.html                   # 基础模板（navbar + 公共结构）
│   └── index.html                  # 主页（侧边栏 + 图表区 + 放大 Modal）
├── static/
│   ├── css/style.css               # 样式
│   └── js/main.js                  # 前端逻辑（图表渲染 + 放大 + 滚动联动）
└── libs/                            # 备用依赖目录（默认为空）
```

## 运行方式

### 方式一：直接运行 Python 源码（开发/调试）

**环境要求**：Python 3.13+，已安装依赖（见 `requirements.txt`）

**步骤**：

1. 双击 `start.bat`（推荐），或在 PowerShell 中执行：

```powershell
$env:DATA_DIR="$env:TEMP\ta_data"
$env:PYTHONDONTWRITEBYTECODE="1"
Set-Location "d:\Code\CodePython\traecode-测试分析001"
& "C:\Users\凯伦\AppData\Local\Programs\Python\Python313\python.exe" -B app.py
```

2. 浏览器访问：**http://127.0.0.1:5000/**
3. 按 `Ctrl+C` 停止服务

### 方式二：打包成 EXE 后运行（部署/分发）

**生成 EXE**：

```powershell
Set-Location "d:\Code\CodePython\traecode-测试分析001"
& "C:\Users\凯伦\AppData\Local\Programs\Python\Python313\python.exe" -m PyInstaller ^
    --noconfirm --clean --onefile --name "测试分析系统" ^
    --add-data "templates;templates" ^
    --add-data "static;static" ^
    --add-data "configs;configs" ^
    --hidden-import flask_sqlalchemy ^
    --hidden-import openpyxl ^
    --hidden-import sqlalchemy ^
    app.py
```

**运行 EXE**：

1. 进入 `dist\` 目录，双击 `测试分析系统.exe`
2. 控制台会显示访问地址：`http://127.0.0.1:5000/`
3. 浏览器打开该地址即可使用
4. 关闭控制台窗口即停止服务

> **注意**：EXE 启动需要几秒初始化，请耐心等待控制台出现 "Running on http://..." 字样

## 依赖清单（requirements.txt）

| 包名 | 版本 | 用途 |
|------|------|------|
| Flask | >=3.0.3 | Web 框架 |
| Flask-SQLAlchemy | >=3.1.1 | ORM |
| pandas | >=2.2.3 | 数据处理 |
| openpyxl | >=3.1.5 | Excel 读取 |
| SQLAlchemy | >=2.0.30 | 数据库引擎 |
| PyMySQL | >=1.1.0 | MySQL 驱动（可选） |
| werkzeug | >=3.0.3 | WSGI 工具库 |

**安装命令**：
```powershell
& "C:\Users\凯伦\AppData\Local\Programs\Python\Python313\python.exe" -m pip install -r requirements.txt
```

## 使用流程

1. 启动应用（见上方"运行方式"）
2. 浏览器打开 http://127.0.0.1:5000/
3. 左侧边栏：
   - 点击"选择 Excel 文件"，选择 `.xlsx` 测试记录
   - 产品类别选择 "-- 默认配置 --"（自动从文件提取配置）
   - 点击"上传并分析"
4. 等待 20-30 秒解析完成，32 张图表自动显示
5. 交互操作：
   - 左侧筛选树：勾选/取消图表或曲线
   - 滚动页面：侧边栏自动高亮+滚动到对应图表
   - 点击放大按钮：全屏查看图表详情，可下载 PNG

## 关键特性

| 功能 | 说明 |
|------|------|
| 自动配置提取 | 无需预置配置，从 Excel 文件自动识别标题行、固定列、规格上下限 |
| 数据分组规则 | 按 `(spec_lower, spec_upper)` 元组合并相同规格的项目到同一图表 |
| 滚动联动 | 页面滚动时左侧边栏自动高亮+滚动到对应图表项（IntersectionObserver） |
| 图表放大 | 每个图表右上角放大按钮，全屏高清查看 + 下载 PNG |
| 公共信息 | Program Name 在 navbar 和卡片头部双重显示 |
| 参考线 | 规格上下限=绿色实线，均值=粉红色虚线，标签内置防裁切 |
| Y 轴范围 | min(规格下限, 数据最小)-8% ~ max(规格上限, 数据最大)+8% |

## 数据存储

- **数据库**：默认 SQLite（`test_analysis.db`），位于 `DATA_DIR` 环境变量指向的目录
- **上传文件**：`{DATA_DIR}/uploads/`，保留原始 Excel 与序列化 DataFrame
- **配置文件**：`configs/*.json`（只读模板）+ `{DATA_DIR}/configs/`（用户保存的配置）
- **环境变量**：
  - `DATA_DIR`：数据写入目录（沙箱环境下设为 `%TEMP%\ta_data`）
  - `DATABASE_URL`：数据库连接字符串（部署到 MySQL 时使用）
  - `SECRET_KEY`：Flask 密钥

## 部署到服务器

1. 修改 `config.py` 中 `SQLALCHEMY_DATABASE_URI` 为 MySQL 连接串，或设置环境变量 `DATABASE_URL`
2. 使用生产级 WSGI 服务器（如 waitress）：
   ```powershell
   pip install waitress
   waitress-serve --host=0.0.0.0 --port=5000 app:app
   ```
3. 反向代理（Nginx）+ HTTPS 配置
